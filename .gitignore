# Rust
/target/
**/*.rs.bk

# Release/build outputs (generated by scripts/workflows)
/dist/
.smoke-bin/
*.log

# Node (UI)
node_modules/

# NOTE: `ui/dist/` is embedded into the Rust binary (see `src/server/mod.rs`),
# so we intentionally do NOT ignore it here.
/ui/node_modules/
/ui/.vite/
/ui/.cache/

# OS / editor junk
.DS_Store
.idea/
.vscode/
*.swp

# Local env
.env
.env.*

# Local toolchains used for cross-compilation during development
.tools/

# Cloud SQL Auth Proxy local telemetry identifier (created in CWD)
telemetry-id

# Local notes
AGENTS.md
TODO.local.md
HANDOFF_WINDOWS.md
docs/HANDOFF_DEV.md
.local/
